import 'package:flutter/material.dart';

class ChatWithAdmin extends StatelessWidget {
  const ChatWithAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(),);
  }
}