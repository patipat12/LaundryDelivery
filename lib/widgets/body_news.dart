import 'package:flutter/material.dart';

class BodyNews extends StatelessWidget {
  const BodyNews({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('This is News');
  }
}