import 'package:flutter/material.dart';

class BodyHistort extends StatelessWidget {
  const BodyHistort({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("this is history");
  }
}