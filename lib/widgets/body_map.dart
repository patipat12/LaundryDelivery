import 'package:flutter/material.dart';

class BodyMap extends StatelessWidget {
  const BodyMap({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('This is map');
  }
}